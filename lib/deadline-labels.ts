/**
 * What a page shows for a bid date that is in the bid documents rather than
 * in our data. A module of its own so client components can import the label
 * without pulling in the source rules behind lib/deadline-in-documents.ts.
 */
export const DEADLINE_IN_DOCUMENTS_LABEL = "见招标文件";
